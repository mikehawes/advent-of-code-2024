use crate::day14::tile_floor::TileFloor;

pub struct Robots {}

impl Robots {
    pub fn parse(_: &str) -> Robots {
        Robots {}
    }
    pub fn safety_factor_after_seconds(&self, _: usize, _: TileFloor) -> usize {
        0
    }
}
